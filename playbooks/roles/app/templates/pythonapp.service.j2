[Unit]
Description=Python Flask Application
After=network.target

[Service]
User={{ app_owner }}
WorkingDirectory={{ app_path }}
ExecStart={{ python_interpreter }} {{ app_path }}/app.py
Restart=always

[Install]
WantedBy=multi-user.target